// This component is deprecated and has been replaced by AuthScreen.tsx
// It is kept to avoid breaking the build process if referenced elsewhere, but it is not used in the active application flow.
import React from 'react';
const DeprecatedRegistrationScreen: React.FC = () => null;
export default DeprecatedRegistrationScreen;
